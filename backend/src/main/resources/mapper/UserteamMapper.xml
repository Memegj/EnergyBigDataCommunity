<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="upc.backend.mapper.UserteamMapper">
    <resultMap id="BaseResultMap" type="upc.backend.entity.Userteam">
        <id column="User_teamId" jdbcType="INTEGER" property="User_teamId" />
        <result column="UserId" jdbcType="INTEGER" property="UserId" />
        <result column="TeamId" jdbcType="INTEGER" property="TeamId" />
    </resultMap>
    <sql id="Base_Column_List">
        User_teamId, UserId, TeamId
    </sql>

    <select id="selectByUser_teamId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from user_team
        where User_teamId = #{User_teamId,jdbcType=INTEGER}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from user_team
        where UserId = #{UserId,jdbcType=INTEGER}
    </select>
    <select id="selectByPageUtil" parameterType="Map" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from user_team
        where UserId = #{userid}
    </select>

    <select id="selectByTeamId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from user_team
        where TeamId = #{TeamId,jdbcType=INTEGER}
    </select>

    <select id="getNumOfTotalUserteam" parameterType="Map" resultType="int">
        select count(*) from user_team
    </select>
    <select id="findAllUserteamList" parameterType="Map" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from user_team
        <if test="start!=null and limit!=null">
            limit #{start},#{limit}
        </if>
    </select>

    <insert id="insert" parameterType="upc.backend.entity.Userteam">
        insert into teamfile (User_teamId, UserId, TeamId)
        values (#{User_teamId,jdbcType=INTEGER}, #{UserId,jdbcType=INTEGER}, #{TeamId,jdbcType=INTEGER}
    </insert>
    <insert id="insertSelective" parameterType="upc.backend.entity.Userteam">
        insert into user_team
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="User_teamId != null">
                User_teamId,
            </if>
            <if test="UserId != null">
                UserId,
            </if>
            <if test="TeamId != null">
                TeamId,
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="User_teamId != null">
                #{User_teamId, jdbcType=INTEGER},
            </if>
            <if test="UserId != null">
                #{UserId, jdbcType=INTEGER},
            </if>
            <if test="TeamId != null">
                #{TeamId, jdbcType=INTEGER},
            </if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="upc.backend.entity.Userteam">
        update user_team
        <set>
            <if test="UserId != null">
                UserId = #{UserId,jdbcType=INTEGER},
            </if>
            <if test="TeamId != null">
                TeamId = #{TeamId,jdbcType=INTEGER},
            </if>
        </set>
        where User_teamId = #{User_teamId,jdbcType=INTEGER}
    </update>
    <update id="updateByPrimaryKey" parameterType="upc.backend.entity.Userteam">
        update user_team
        set UserId = #{UserId,jdbcType=INTEGER},
            TeamId = #{TeamId,jdbcType=INTEGER}
        where User_teamId = #{User_teamId,jdbcType=INTEGER}
    </update>
    <delete id="deleteBatch" parameterType="java.lang.Integer">
        delete from user_team
        where User_teamId in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>
</mapper>